{
  "name": "dockenv",
  "version": "1.0.1",
  "description": "Dockenv is a command-line tool for converting .NET user secrets JSON files to .env files for use with Docker. It simplifies the process of setting up environment variables in Docker containers when working with .NET projects.",
  "main": "index.js",
  "bin": {
    "dockenv": "index.js"
  },
  "scripts": {
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^10.0.1"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
